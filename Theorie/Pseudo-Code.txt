tokens[] = vscode extraction...      // récupérations des tokens via lsp  
linesList = linesOfToken(tokens[])   // fonction d'extraction des numéros de ligne des tokes (fait parti de la structure de semantic-token-sample)  
tokens[] = tri(tokens[])     // tri des tokens si nécessaire 



for i in range( len(tokens[])):
  nLine = lineList[i] 
  token = tokens[i] 
  write("class " + token + " { ", outputFile) 
  line = file.read(sourceFile, nLine)     // récupère le contenu de la ligne du token 
  temp = line.split(":") 
  line = temp[1] 
  for w in line :
    if w in tokens[] :
       linkprocess.append(w) 
    else 
            // repérer variable et type pour l'ecrire dans la class 
    for l in linkprocess :
      write( token + " <=down " + l, outputFile) 




à essaye de fare en typescript pour l'implémenter
